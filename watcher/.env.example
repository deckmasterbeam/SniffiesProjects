# Base URL of the deployed server (no trailing slash).
SERVER_BASE=https://sniffies-projects.vercel.app

# Bearer token for watcher-facing API endpoints. Keep this private,
# it is never distributed and should only exist in your local watcher build.
WATCHER_SECRET=your-watcher-secret-here
